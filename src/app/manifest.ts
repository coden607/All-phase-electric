import type { MetadataRoute } from 'next';
export default function manifest(): MetadataRoute.Manifest { return { name:'All Phase Electric Estimate Intake', short_name:'All Phase Intake', description:'Secure customer estimate request and lead follow-up workflow.', start_url:'/estimate', display:'standalone', background_color:'#f4f7fb', theme_color:'#0b2a4a', icons:[] }; }
